## 一键启动与停止

双击根目录的 `start-local.bat` 会静默启动 Vite 和当前仓库的 Canvas Agent 源码，适合本地开发和协议联调。双击 `start-release.bat` 会静默启动 Vite 和 npm 已发布的 Canvas Agent，适合发布后的验证。

两个启动脚本共用 `stop-local.bat`，它会结束当前一键启动的 Vite、Canvas Agent 及其子进程，并清理 `3000`、`17371` 端口上的遗留服务。每次点击启动脚本都会重启 Agent，确保启动方式与协议版本生效；Vite 会复用已有进程。脚本会在后台确认 Agent 已监听 `17371` 且协议版本匹配，失败时弹出错误和启动日志路径。

## 手动启动

1. 启动本地项目(不是GitHub在线版，使用github在线版就不需要启动本项目了)：
1.1 进入文件目录
cd web
或者
cd E:\pyproject\infinite-canvas\web
1.2 启动
bun run dev

2. 本仓库源码开发时，在 PowerShell 启动 Canvas Agent：

```powershell
cd canvas-agent
bun install --frozen-lockfile
bun src/index.ts
```

3. 验证 npm 已发布版本时，在 PowerShell 启动 Canvas Agent：

```powershell
npx.cmd --yes @basketikun/canvas-agent@latest
```

【ps：npx.cmd --yes --prefer-online @basketikun/canvas-agent@latest
--prefer-online 适合这次这种缓存或版本异常后的强制更新；日常启动不必带它，否则每次都会优先访问 npm。】
